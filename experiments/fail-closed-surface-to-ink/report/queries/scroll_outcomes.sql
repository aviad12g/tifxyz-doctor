-- Frozen release snapshot derived from evidence/breadth-status.json.
SELECT 'Clean ≥0.5 cm² raw surface; review negative' AS outcome, 4 AS count
UNION ALL
SELECT 'Clean patch below internal raw-render gate', 5
UNION ALL
SELECT 'No established clean patch / boundary failure', 4;
