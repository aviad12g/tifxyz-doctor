-- Frozen release snapshot derived from evidence/breadth-status.json and the
-- PHerc0813 validation records listed in RELEASE_PROVENANCE.json.
SELECT
  13 AS eligible_scrolls,
  13 AS covered_scrolls,
  4 AS raw_screened_scrolls,
  0 AS defensible_candidates,
  0.536269 AS pherc0813_area_cm2;
