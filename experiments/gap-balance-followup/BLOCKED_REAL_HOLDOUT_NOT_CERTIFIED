GapBalance training is blocked.

PHerc1218 now passes the physical-scroll and checkpoint-lineage independence
checks: Dataset059/source-checkpoint training used Scrolls 1, 4, and 5, while
PHerc1218 is a separate physical scroll. The new labels and crop-selection rule
also predate every GapBalance prediction.

The current Kaggle dataset version is NOT freezeable, however. Its manifest
contains 260 contact crops with band counts 17/60/60/60/63, while the frozen
extraction summary and public results specify 254 crops with counts
14/60/60/60/60. The extraction script reused a non-empty output directory and
downstream packaging globbed every NPZ in that directory, so six stale files
are a plausible explanation. The dataset author has been asked to confirm the
authoritative membership and publish a corrected immutable version.

Do not train Control, Gap2, or Gap4 until all of the following are true:
- the corrected Kaggle version and exact manifest SHA-256 are frozen;
- its manifest reconciles exactly with the extraction summary;
- the metadata-only eligibility and fixed-panel lists are generated and frozen;
- the complete protocol validator passes; and
- Aviad has reviewed the timed compute estimate and explicitly approved training.
